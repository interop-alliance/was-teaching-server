# `./data/spaces` Directory

This is where the `was-teaching-server` keeps the data (as files in folders)
for its Spaces.

The folder is deliberately added to the repo's `.gitignore` so that the actual
contents of the spaces don't get added to Git.
